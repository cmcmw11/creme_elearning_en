/* /assets/css/style.css */

/* ใช้ฟอนต์ Sarabun */
body {
  font-family: 'Sarabun', sans-serif;
  margin: 0;
  padding: 0;
  background: linear-gradient(to bottom right, #F9F7FF, #D3F5FF);
  display: flex;
  justify-content: center;
  align-items: flex-start;
  min-height: 100vh;
  padding-top: 20px;
}

/* Container สำหรับทั้งหมด */
.container {
  width: 100%;
  max-width: 600px; /* ขนาดเหมาะสมกับมือถือ */
  background-color: #fff;
  padding: 20px;
  border-radius: 12px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.1);
}

/* Header ที่ด้านบน */
.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
}

.header h2 {
  font-size: 1.6rem;
  color: #4C6BFF; /* สีฟ้าสดใส */
}

.header button {
  padding: 0.7rem 1.5rem;
  background-color: #6C63FF; /* สีม่วง */
  color: white;
  border: none;
  border-radius: 1rem;
  cursor: pointer;
  font-size: 1rem;
}

.header button:hover {
  background-color: #5A52F5; /* สีม่วงเข้ม */
}

/* Feed ที่แสดงคำศัพท์หรือข้อมูลต่างๆ */
.feed {
  background-color: #fff;
  padding: 15px;
  border-radius: 10px;
  box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.feed .post {
  margin-bottom: 15px;
}

.feed .post h3 {
  font-size: 1.3rem;
  color: #4C6BFF; /* ฟ้าสดใส */
}

.feed .post p {
  font-size: 1rem;
  color: #555;
  margin-top: 10px;
}

.feed .post button {
  padding: 0.5rem 1.2rem;
  background-color: #77C9B1; /* สีเขียวอ่อน */
  color: white;
  font-size: 1rem;
  border: none;
  border-radius: 1rem;
  cursor: pointer;
  margin-top: 10px;
  transition: background-color 0.3s;
}

.feed .post button:hover {
  background-color: #64B0A4; /* สีเขียวเข้ม */
}

/* Media Query สำหรับมือถือ */
@media (max-width: 768px) {
  .header h2 {
    font-size: 1.4rem;
  }

  .feed .post h3 {
    font-size: 1.2rem;
  }

  .feed .post p {
    font-size: 0.9rem;
  }
}

/* เพิ่มอิโมจิ */
.emoji {
  font-size: 1.5rem;
  margin-right: 10px;
}
